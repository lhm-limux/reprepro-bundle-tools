import { SortedKeysPipe } from './sorted-keys.pipe';

describe('SortedKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new SortedKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
